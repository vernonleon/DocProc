# DocProc
Local-first desktop app for securely converting sensitive PDF records to Markdown via vision models (LM Studio). Features a PDF→PNG pipeline with record-level isolation to prevent data contamination and minimize PHI exposure. Supports OpenAI-compatible APIs, concurrent processing, and strict network allowlists for maximum privacy.
